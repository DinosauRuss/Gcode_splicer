
Program to combine 2 Slic3r Gcode files at a specified layer height. Useful for having different sections of the model have different settings.

'Layer Height' is the layer at which the files will be merged at. Both files must have this exact layer height.

'First File' will contain the layers below the split, 'Second File' will contain the layers above.

GcodeCombo_cmdline.py is a command line version.
GcodeCombo_gui.py is a tkinter gui version of the same program.
